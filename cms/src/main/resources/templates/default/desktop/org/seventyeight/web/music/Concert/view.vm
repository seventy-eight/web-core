
<div id="container">
    #foreach($id in $item.getArtistIds())
        <div id="$id" class="artist" style="float: left">
        Loading
        </div>
    #end
</div>

<div class="ui-widget" style="width: 100%">
    <input id="artistInput">
</div>

<br>


<script type="text/javascript">

    var callback = function(id){ return '<div class="newResource" id="' + id + '" style="float:left">Waiting for ' + id + '</div>' };
    //$("#container").append(callback("hej"));
    Utils.resourceListHandler('container', 'artistInput', '/artist/getArtists', 'addArtist', 'artist', callback);


    /*
    $(function() {
        $('.artist').each(function(index) {
            getArtistView(this.id);
        });
    });
    function getArtistView(id) {
        $.ajax({
            type: "GET",
            url: "/resource/" + id + "/getView?view=wide",
            success: function(data, textStatus, jqxhr){
                $('#' + id).html(data);
            },
            error: function(ajax, text, error) {alert(error)}
        });
    }
    */
</script>