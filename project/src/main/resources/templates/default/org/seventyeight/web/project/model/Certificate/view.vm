
#if( $request.isAuthenticated() )
    #if($item.hasSkill($request.getUser()))
        You have this skill
    #else
        <a href="" class="button" id="addCert">I have this skill</a>
    #end
#end



$item.getProfiles()


<script type="text/javascript">
    $('#addCert').click(function(e) {
        e.preventDefault();
        $.ajax({
            type: "POST",
            url: "/profile/$request.getUser().getIdentifier()/skills/add",
            data: { skillTitle: "$item.getDisplayName()" },
            success: function(data, textStatus, jqxhr){$('#addCert').hide(0)},
            error: function(ajax, text, error) {alert(error)}
        });
    })
</script>